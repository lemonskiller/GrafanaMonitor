# Secrets

Create `grafana-admin-password` and `smtp-password` in this directory before starting the stack. For the configured 163.com sender, `smtp-password` must contain the SMTP client authorization code, not the web login password. Files in this directory are ignored by Git, except for this instruction file.
